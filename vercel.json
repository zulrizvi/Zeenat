{
		"builds": [
			{
				"src": "react-page-flip-main/package.json",
				"use": "@vercel/static-build",
				"config": {
					"distDir": "react-page-flip-main/dist"
				}
			}
		],
	"routes": [
		{ "handle": "filesystem" },
		{ "src": "/.*", "dest": "/index.html" }
	]
}
